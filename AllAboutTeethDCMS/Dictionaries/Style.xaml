<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:root="clr-namespace:AllAboutTeethDCMS"
                    xmlns:Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero">
    <Style x:Key="PrimaryButton" TargetType="{x:Type Button}">
        <Setter Property="FontFamily" Value="Century Gothic"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="Background" Value="{StaticResource Turquoise}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid>
                        <Border CornerRadius="3" Background="{TemplateBinding Background}" Margin="5" SnapsToDevicePixels="True">
                            <ContentPresenter Margin="5" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DangerButton" TargetType="{x:Type Button}" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="Background" Value="{StaticResource Alizarin}"/>
    </Style>

    <Style x:Key="InfoButton" TargetType="{x:Type Button}" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="Background" Value="{StaticResource PeterRiver}"/>
    </Style>

    <Style x:Key="WarningButton" TargetType="{x:Type Button}" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="Background" Value="{StaticResource Sunflower}"/>
    </Style>

    <Style x:Key="TextBox" TargetType="{x:Type TextBox}">
        <Setter Property="FontFamily" Value="Century Gothic"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="SelectionBrush" Value="{StaticResource Turquoise}"/>
        <Setter Property="CaretBrush" Value="{StaticResource Turquoise}"/>
        <Setter Property="Foreground" Value="{StaticResource WetAsphalt}"/>
        <Setter Property="Background" Value="{StaticResource Turquoise}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Turquoise}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <StackPanel SnapsToDevicePixels="True">
                        <Border>
                            <Label Content="{TemplateBinding root:AttachedProperties.Label}" FontSize="13" VerticalAlignment="Center" Foreground="{TemplateBinding BorderBrush}"/>
                        </Border>
                        <Border CornerRadius="3" BorderBrush="{TemplateBinding Background}" BorderThickness="2" Margin="5,5,5,0">
                            <ScrollViewer x:Name="PART_ContentHost" SnapsToDevicePixels="True" Margin="5"/>
                        </Border>
                        <Border>
                            <Label Content="{TemplateBinding root:AttachedProperties.Error}" FontSize="11" VerticalAlignment="Center" Foreground="{StaticResource Alizarin}"/>
                        </Border>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="NotRequiredTextBox" TargetType="{x:Type TextBox}" BasedOn="{StaticResource TextBox}">
        <Setter Property="SelectionBrush" Value="{StaticResource Asbestos}"/>
        <Setter Property="CaretBrush" Value="{StaticResource Asbestos}"/>
        <Setter Property="Foreground" Value="{StaticResource WetAsphalt}"/>
        <Setter Property="Background" Value="{StaticResource Asbestos}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Asbestos}"/>
    </Style>

    <Style x:Key="PasswordBox" TargetType="{x:Type PasswordBox}">
        <Setter Property="FontFamily" Value="Century Gothic"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="SelectionBrush" Value="{StaticResource Turquoise}"/>
        <Setter Property="CaretBrush" Value="{StaticResource Turquoise}"/>
        <Setter Property="Foreground" Value="{StaticResource WetAsphalt}"/>
        <Setter Property="Background" Value="{StaticResource Turquoise}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Turquoise}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type PasswordBox}">
                    <StackPanel SnapsToDevicePixels="True">
                        <Border>
                            <Label Content="{TemplateBinding root:AttachedProperties.Label}" FontSize="13" VerticalAlignment="Center" Foreground="{TemplateBinding BorderBrush}"/>
                        </Border>
                        <Border CornerRadius="3" BorderBrush="{TemplateBinding Background}" BorderThickness="2" Margin="5,5,5,0">
                            <ScrollViewer x:Name="PART_ContentHost" SnapsToDevicePixels="True" Margin="5"/>
                        </Border>
                        <Border>
                            <Label Content="{TemplateBinding root:AttachedProperties.Error}" FontSize="11" VerticalAlignment="Center" Foreground="{StaticResource Alizarin}"/>
                        </Border>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ItemContainerStyle" TargetType="{x:Type ComboBoxItem}">
        <Setter Property="Foreground" Value="{StaticResource WetAsphalt}"/>
        <Setter Property="Background" Value="{StaticResource Clouds}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Border Background="{TemplateBinding Background}" Padding="5" SnapsToDevicePixels="True">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter Property="Background" Value="{StaticResource Silver}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ComboBox" TargetType="{x:Type ComboBox}">
        <Setter Property="Background" Value="{StaticResource Turquoise}"/>
        <Setter Property="FontFamily" Value="Century Gothic"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource ItemContainerStyle}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <StackPanel>
                        <Border>
                            <Label Content="{TemplateBinding root:AttachedProperties.Label}" FontSize="13" VerticalAlignment="Center" Foreground="{StaticResource Turquoise}"/>
                        </Border>
                        <Border Background="{TemplateBinding Background}" Margin="5,5,5,0" CornerRadius="3" SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Popup x:Name="PART_Popup" AllowsTransparency="True" Grid.ColumnSpan="2" IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}" PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}" PlacementTarget="{Binding ElementName=root}" Placement="Bottom">
                                    <Border x:Name="DropDownBorder" SnapsToDevicePixels="True" Background="Transparent">
                                        <ScrollViewer x:Name="DropDownScrollViewer">
                                            <Border RenderOptions.ClearTypeHint="Enabled">
                                                <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                            </Border>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                                <ContentPresenter x:Name="presenter" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" Margin="10,0,10,0" Content="{TemplateBinding SelectionBoxItem}" ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" IsHitTestVisible="False" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center"/>
                                <ToggleButton Margin="5,5,5,6" Grid.ColumnSpan="2" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ToggleButton.Style>
                                        <Style TargetType="{x:Type ToggleButton}">
                                            <Setter Property="OverridesDefaultStyle" Value="True"/>
                                            <Setter Property="IsTabStop" Value="False"/>
                                            <Setter Property="Focusable" Value="False"/>
                                            <Setter Property="ClickMode" Value="Press"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                                        <Grid Background="Transparent" IsHitTestVisible="True">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <Path Data="M0,0L3.5,4 7,0z" Grid.Column="1" Fill="White" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10"/>
                                                        </Grid>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                            </Grid>
                        </Border>
                        <Border>
                            <Label Content="{TemplateBinding root:AttachedProperties.Error}" FontSize="11" VerticalAlignment="Center" Foreground="{StaticResource Alizarin}"/>
                        </Border>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="DatePickerStyle" TargetType="{x:Type DatePicker}">
        <Setter Property="Foreground" Value="{StaticResource WetAsphalt}"/>
        <Setter Property="IsTodayHighlighted" Value="True"/>
        <Setter Property="SelectedDateFormat" Value="Short"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{StaticResource Turquoise}"/>
        <Setter Property="FontFamily" Value="Century Gothic"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="Validation.ErrorTemplate" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DatePicker}">
                    <StackPanel>
                        <Border>
                            <Label Content="{TemplateBinding root:AttachedProperties.Label}" FontSize="13" VerticalAlignment="Center" Foreground="{TemplateBinding BorderBrush}"/>
                        </Border>
                        <Border BorderBrush="{TemplateBinding BorderBrush}" CornerRadius="3" SnapsToDevicePixels="True" BorderThickness="2" Background="Transparent" Margin="5,5,5,0">
                            <Grid x:Name="PART_Root">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Button x:Name="PART_Button" Grid.Column="1" Foreground="{TemplateBinding Foreground}" Focusable="False" HorizontalAlignment="Left" Grid.Row="0" VerticalAlignment="Center" Width="20" Margin="0,0,5,0">
                                    <Button.Style>
                                        <Style TargetType="{x:Type Button}">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type Button}">
                                                        <Border>
                                                            <Path Fill="{StaticResource Turquoise}" Data="M 4.800781 0 C 4.363281 0 4 0.363281 4 0.800781 L 4 1.601563 L 1.601563 1.601563 C 1.136719 1.601563 0.800781 1.9375 0.800781 2.398438 L 0.800781 5.199219 L 19.199219 5.199219 L 19.199219 2.398438 C 19.199219 1.9375 18.863281 1.601563 18.398438 1.601563 L 16 1.601563 L 16 0.800781 C 16 0.363281 15.636719 0 15.199219 0 L 14.398438 0 C 13.960938 0 13.601563 0.363281 13.601563 0.800781 L 13.601563 1.601563 L 6.398438 1.601563 L 6.398438 0.800781 C 6.398438 0.363281 6.039063 0 5.601563 0 Z M 4.800781 0.800781 L 5.601563 0.800781 L 5.601563 3.199219 L 4.800781 3.199219 Z M 14.398438 0.800781 L 15.199219 0.800781 L 15.199219 3.199219 L 14.398438 3.199219 Z M 0.800781 6 L 0.800781 18.398438 C 0.800781 18.863281 1.136719 19.199219 1.601563 19.199219 L 18.398438 19.199219 C 18.863281 19.199219 19.199219 18.863281 19.199219 18.398438 L 19.199219 6 Z M 4.800781 8.398438 L 6.800781 8.398438 L 6.800781 10.398438 L 4.800781 10.398438 Z M 7.601563 8.398438 L 9.601563 8.398438 L 9.601563 10.398438 L 7.601563 10.398438 Z M 10.398438 8.398438 L 12.398438 8.398438 L 12.398438 10.398438 L 10.398438 10.398438 Z M 13.199219 8.398438 L 15.199219 8.398438 L 15.199219 10.398438 L 13.199219 10.398438 Z M 4.800781 11.199219 L 6.800781 11.199219 L 6.800781 13.199219 L 4.800781 13.199219 Z M 7.601563 11.199219 L 9.601563 11.199219 L 9.601563 13.199219 L 7.601563 13.199219 Z M 10.398438 11.199219 L 12.398438 11.199219 L 12.398438 13.199219 L 10.398438 13.199219 Z M 13.199219 11.199219 L 15.199219 11.199219 L 15.199219 13.199219 L 13.199219 13.199219 Z M 4.800781 14 L 6.800781 14 L 6.800781 16 L 4.800781 16 Z M 7.601563 14 L 9.601563 14 L 9.601563 16 L 7.601563 16 Z M 10.398438 14 L 12.398438 14 L 12.398438 16 L 10.398438 16 Z M 13.199219 14 L 15.199219 14 L 15.199219 16 L 13.199219 16 Z "/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <DatePickerTextBox x:Name="PART_TextBox" Margin="0,1,0,1" Grid.Column="0" Focusable="{TemplateBinding Focusable}" HorizontalContentAlignment="Stretch" Grid.Row="0">
                                    <DatePickerTextBox.Style>
                                        <Style TargetType="{x:Type DatePickerTextBox}">
                                            <Setter Property="Padding" Value="5"/>
                                            <Setter Property="SelectionBrush" Value="{StaticResource Turquoise}"/>
                                            <Setter Property="CaretBrush" Value="{StaticResource Turquoise}"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type DatePickerTextBox}">
                                                        <Grid>
                                                            <Border VerticalAlignment="Center" SnapsToDevicePixels="True">
                                                                <ScrollViewer x:Name="PART_ContentHost" SnapsToDevicePixels="True"/>
                                                            </Border>
                                                        </Grid>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </DatePickerTextBox.Style>
                                </DatePickerTextBox>
                                <Grid x:Name="PART_DisabledVisual" Grid.ColumnSpan="2" Grid.Column="0" IsHitTestVisible="False" Opacity="0" Grid.Row="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Rectangle Grid.Column="0" Fill="#A5FFFFFF" RadiusY="1" Grid.Row="0" RadiusX="1"/>
                                    <Rectangle Grid.Column="1" Fill="#A5FFFFFF" Height="18" Margin="3,0,3,0" RadiusY="1" Grid.Row="0" RadiusX="1" Width="19"/>
                                    <Popup x:Name="PART_Popup" AllowsTransparency="True" Placement="Bottom" PlacementTarget="{Binding ElementName=PART_TextBox}" StaysOpen="False"/>
                                </Grid>
                            </Grid>
                        </Border>
                        <Border>
                            <Label Content="{TemplateBinding root:AttachedProperties.Error}" FontSize="11" VerticalAlignment="Center" Foreground="{StaticResource Alizarin}"/>
                        </Border>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding Source={x:Static SystemParameters.HighContrast}}" Value="false">
                            <Setter Property="Foreground" TargetName="PART_TextBox" Value="{Binding Foreground, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="DatagridStyle" TargetType="{x:Type DataGrid}">
        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
            <ScrollViewer x:Name="DG_ScrollViewer" Focusable="False">
                <ScrollViewer.Template>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Button Command="ApplicationCommands.SelectAll" Focusable="False" Style="{DynamicResource {ComponentResourceKey ResourceId=DataGridSelectAllButtonStyle, TypeInTargetAssembly={x:Type DataGrid}}}" Width="{Binding CellsPanelHorizontalOffset, RelativeSource={RelativeSource FindAncestor, AncestorLevel=1, AncestorType={x:Type DataGrid}}}">
                                <Button.Visibility>
                                    <Binding Path="HeadersVisibility" RelativeSource="{RelativeSource FindAncestor, AncestorLevel=1, AncestorType={x:Type DataGrid}}">
                                        <Binding.ConverterParameter>
                                            <DataGridHeadersVisibility>All</DataGridHeadersVisibility>
                                        </Binding.ConverterParameter>
                                    </Binding>
                                </Button.Visibility>
                            </Button>
                            <DataGridColumnHeadersPresenter x:Name="PART_ColumnHeadersPresenter" Grid.Column="1">
                                <DataGridColumnHeadersPresenter.Visibility>
                                    <Binding Path="HeadersVisibility" RelativeSource="{RelativeSource FindAncestor, AncestorLevel=1, AncestorType={x:Type DataGrid}}">
                                        <Binding.ConverterParameter>
                                            <DataGridHeadersVisibility>Column</DataGridHeadersVisibility>
                                        </Binding.ConverterParameter>
                                    </Binding>
                                </DataGridColumnHeadersPresenter.Visibility>
                            </DataGridColumnHeadersPresenter>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" CanContentScroll="{TemplateBinding CanContentScroll}" CanHorizontallyScroll="False" Grid.ColumnSpan="2" CanVerticallyScroll="False" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" ContentStringFormat="{TemplateBinding ContentStringFormat}" Grid.Row="1"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar" Grid.Column="2" Maximum="{TemplateBinding ScrollableHeight}" Orientation="Vertical" Grid.Row="1" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" ViewportSize="{TemplateBinding ViewportHeight}"/>
                            <Grid Grid.Column="1" Grid.Row="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="{Binding NonFrozenColumnsViewportHorizontalOffset, RelativeSource={RelativeSource FindAncestor, AncestorLevel=1, AncestorType={x:Type DataGrid}}}"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <ScrollBar x:Name="PART_HorizontalScrollBar" Grid.Column="1" Maximum="{TemplateBinding ScrollableWidth}" Orientation="Horizontal" Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" ViewportSize="{TemplateBinding ViewportWidth}"/>
                            </Grid>
                        </Grid>
                    </ControlTemplate>
                </ScrollViewer.Template>
                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
            </ScrollViewer>
        </Border>
    </ControlTemplate>

    <Style x:Key="Row" TargetType="{x:Type DataGridRow}">
        <Setter Property="FontFamily" Value="Century Gothic"/>
        <Setter Property="Foreground" Value="{StaticResource MidnightBlue}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridRow}">
                    <Border SnapsToDevicePixels="True" Background="{TemplateBinding Background}">
                        <SelectiveScrollingGrid>
                            <SelectiveScrollingGrid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </SelectiveScrollingGrid.ColumnDefinitions>
                            <SelectiveScrollingGrid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </SelectiveScrollingGrid.RowDefinitions>
                            <DataGridCellsPresenter Grid.Column="1"
                                                ItemsPanel="{TemplateBinding ItemsPanel}"
                                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </SelectiveScrollingGrid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource Sunflower}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="cell" TargetType="{x:Type DataGridCell}">
        <Setter Property="BorderThickness" Value="0"/>
    </Style>

    <Style x:Key="Header" TargetType="{x:Type DataGridColumnHeader}">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontFamily" Value="Century Gothic"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="Background" Value="{StaticResource Turquoise}"/>
        <Setter Property="SeparatorBrush" Value="{StaticResource MidnightBlue}"/>
        <Setter Property="SeparatorVisibility" Value="Visible"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                    <Grid>
                        <Border Background="{TemplateBinding Background}" SnapsToDevicePixels="True" BorderBrush="{StaticResource WetAsphalt}" BorderThickness="0,0,1,1">
                            <ContentPresenter HorizontalAlignment="Center" Margin="5"/>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="SortDirection" Value="Ascending">
                            <Setter Property="Background" Value="{StaticResource GreenSea}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DataGrid" TargetType="{x:Type DataGrid}">
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource Header}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WetAsphalt}"/>
        <Setter Property="CellStyle" Value="{StaticResource cell}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="RowStyle" Value="{StaticResource Row}"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
    </Style>

    <Style x:Key="CheckBox" TargetType="{x:Type CheckBox}">
        <Setter Property="FontFamily" Value="Century Gothic"/>
        <Setter Property="Foreground" Value="{StaticResource WetAsphalt}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">
                    <Grid Margin="5" SnapsToDevicePixels="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border x:Name="box" Grid.Column="1" Background="{StaticResource Concrete}" CornerRadius="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border x:Name="left" Margin="3" Width="21" Height="21" CornerRadius="12" Background="{StaticResource Asbestos}"/>
                                <Border x:Name="right" Visibility="Collapsed" Margin="3" Grid.Column="2" Width="21" Height="21" CornerRadius="12" Background="{StaticResource Turquoise}"/>
                                <Label HorizontalAlignment="Center" Grid.Column="1" Grid.ColumnSpan="2" x:Name="no" Content="NO" FontFamily="{TemplateBinding FontFamily}" Foreground="{StaticResource Clouds}" FontSize="{TemplateBinding FontSize}"/>
                                <Label HorizontalAlignment="Center" Grid.ColumnSpan="2" x:Name="yes" Visibility="Collapsed" Content="YES" FontFamily="{TemplateBinding FontFamily}" Foreground="{StaticResource Clouds}" FontSize="{TemplateBinding FontSize}"/>
                            </Grid>
                        </Border>
                        <ContentPresenter VerticalAlignment="Center" Margin="5,0,5,0"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="true">
                            <Setter Property="Background" Value="{StaticResource WetAsphalt}" TargetName="box"/>
                            <Setter Property="Visibility" Value="Visible" TargetName="yes"/>
                            <Setter Property="Visibility" Value="Collapsed" TargetName="no"/>
                            <Setter Property="Visibility" Value="Visible" TargetName="right"/>
                            <Setter Property="Visibility" Value="Collapsed" TargetName="left"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>